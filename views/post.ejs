<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <title>게시판</title>
    <link rel="stylesheet" href="/styles.css">
</head>
<body>
<div>
    <h1>게시판</h1>

    <!-- 게시물 작성 폼 -->
    <form action="/post" method="POST">
        <input type="text" name="Title" placeholder="제목" required><p>
            <textarea name="Content" placeholder="내용" required></textarea><p>
            <button type="submit">게시물 작성</button><p>
            <a href="/"> 홈페이지</a> <p>
    </form>

    <h2>게시물 목록</h2>
    <ul>
        <% post.forEach(post => { %>
            <li>
                <h3><%= post.Title %></h3>
                <p><%= post.Content %></p>
                <p>작성자: <%= post.Author %></p>
                <p>작성일: <%= post.Created %></p>

                <!-- 게시물 수정 폼 -->
                <form action="/post/<%= post.Id %>" method="POST" style="display:inline;">
                    <input type="text" name="Title" placeholder="새 제목" required><p>
                        <textarea name="Content" placeholder="새 내용" required></textarea><p>
                        <button type="submit">수정</button>
                </form>

                <!-- 게시물 삭제 폼 -->
                <form action="/post/delete/<%= post.Id %>" method="POST" style="display:inline;">
                    <button type="submit">삭제</button>
                </form>
            </li>
        <% }) %>
    </ul>
</div>
</body>
</html>
